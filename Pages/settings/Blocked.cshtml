@page
@model Gamefilled.Pages.Settings.BlockedModel
@{
    ViewData["Title"] = "Blocked Users";
    ViewData["ActiveSettings"] = "Blocked";
}

<div class="container settings-wrap">
    <div class="row">
        <div class="col-lg-3 d-none d-lg-block">
            <partial name="~/Pages/Settings/_SettingsNav.cshtml" />
        </div>

        <div class="col-lg-9">
            <div class="settings-content">
                <div class="settings-content-header">
                    <h2 class="mb-0">Blocked Users</h2>
                </div>

                <p class="subtitle-text">Placeholder — mais tarde: lista de users bloqueados e botão “unblock”.</p>

                <div class="alert alert-info mt-3">
                    Ainda não implementado.
                </div>
            </div>
        </div>
    </div>
</div>

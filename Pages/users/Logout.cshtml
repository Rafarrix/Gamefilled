@page "/users/sign_out"
@model Microsoft.AspNetCore.Mvc.RazorPages.PageModel

@{
    // A página apenas executa a lógica de limpar a sessão e redirecionar no OnGet do cshtml.cs.
    // Mantemos um conteúdo mínimo porque a navegação fará redirect imediato.
}
<p>Signing out…</p>
